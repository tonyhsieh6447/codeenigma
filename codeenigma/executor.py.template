# This file is auto-generated by codeenigma. Do not edit !!!
# Origin: $filename

import sys
import os

# Ensure we import the correct codeenigma_runtime from the same directory structure
# This prevents importing an incompatible runtime with different encryption keys
_current_file = os.path.abspath(__file__)
_current_dir = os.path.dirname(_current_file)
_parent_dir = os.path.dirname(_current_dir)

# Check if codeenigma_runtime exists in parent directory (local runtime)
_local_runtime_path = os.path.join(_parent_dir, 'codeenigma_runtime')
if os.path.isdir(_local_runtime_path) and _local_runtime_path not in sys.path:
    # Prioritize local runtime by inserting at the beginning of sys.path
    sys.path.insert(0, _parent_dir)

from codeenigma_runtime import execute_secure_code

execute_secure_code($secure_code, globals())
